version: "3.8"

services:
  app:
    image: mcr.microsoft.com/devcontainers/javascript-node:24
    volumes:
      - ./:/workspace
    command: sleep infinity
    depends_on:
      - db
    environment:
      - NODE_ENV=development
      - HOST=0.0.0.0
